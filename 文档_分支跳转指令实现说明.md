# 分支跳转指令（B类/J类）实现详解

## 一、概述

本设计实现了完整的MIPS分支跳转指令集，包括：
- **B类指令（条件分支）**：BEQ、BNE、BGTZ、BLEZ、BLTZ、BGEZ、BLTZAL、BGEZAL
- **J类指令（无条件跳转）**：J、JAL、JR、JALR

所有指令均在五级流水线中实现，支持延迟槽（Delay Slot）机制，并通过完善的冒险检测和前推机制保证正确性。

---

## 二、指令定义与译码

### 2.1 指令在maindec2.v中的定义

**文件位置**：`maindec2.v`（主译码器）

#### B类指令定义

```verilog
// 简单分支指令（基于opcode）
6'b000100: controls <= 19'b0001000_001_0_0_000_000_0; // BEQ
6'b000101: controls <= 19'b0001000_001_0_0_000_001_0; // BNE
6'b000111: controls <= 19'b0001000_001_0_0_000_010_0; // BGTZ
6'b000110: controls <= 19'b0001000_001_0_0_000_011_0; // BLEZ

// REGIMM类分支指令（基于opcode=000001和rt字段）
6'b000001: begin
    case (rt)
        5'b00000: controls <= 19'b0001000_001_0_0_000_100_0; // BLTZ
        5'b00001: controls <= 19'b0001000_001_0_0_000_101_0; // BGEZ
        5'b10000: controls <= 19'b1001000_001_0_0_000_100_1; // BLTZAL
        5'b10001: controls <= 19'b1001000_001_0_0_000_101_1; // BGEZAL
    endcase
end
```

**控制信号含义**（19位）：
```
{regwrite, regdst, alusrc, branch, memwrite, memtoreg, jump, 
 aluop[2:0], hassign, islui, mem_op[2:0], branch_op[2:0], link}
```

- **branch** = 1：表示是分支指令
- **branch_op[2:0]**：分支比较类型
  - 000: BEQ（相等）
  - 001: BNE（不等）
  - 010: BGTZ（大于0）
  - 011: BLEZ（小于等于0）
  - 100: BLTZ（小于0）
  - 101: BGEZ（大于等于0）
- **link** = 1：分支链接指令（BLTZAL/BGEZAL），需要保存返回地址
- **regwrite** = 1：对于BLTZAL/BGEZAL，需要写寄存器$31

#### J类指令定义

```verilog
// 简单跳转指令
6'b000010: controls <= 19'b0000001_000_0_0_000_000_0; // J
6'b000011: controls <= 19'b1000001_000_0_0_000_000_1; // JAL
```

**控制信号含义**：
- **jump** = 1：表示是跳转指令
- **link** = 1：JAL需要保存返回地址到$31
- **regwrite** = 1：JAL需要写寄存器

#### JR/JALR指令定义

JR和JALR是R型指令（opcode=000000），在`controller2.v`中特殊处理：

```verilog
// 在controller2.v中的检测
wire is_jr   = (opD == 6'b000000) && (functD == 6'b001000);
wire is_jalr = (opD == 6'b000000) && (functD == 6'b001001);

assign jumpregD = is_jr | is_jalr;  // 寄存器跳转信号
assign jalrD    = is_jalr;
assign regwriteD = regwriteD_raw & ~is_jr;  // JR不写寄存器
```

---

### 2.2 分支比较逻辑（Branch_Cmp.v）

**文件位置**：`Branch_Cmp.v`

**功能**：在译码阶段（ID）完成分支条件判断，提前确定分支是否跳转。

```verilog
module Branch_Cmp(
    input  wire [31:0] a,        // 寄存器rs的值
    input  wire [31:0] b,        // 寄存器rt的值
    input  wire [2:0]  cmp_op,   // 比较操作类型
    output wire        cmp_result // 比较结果
);
```

**实现逻辑**：
```verilog
wire eq  = (a == b);             // 相等判断
wire neq = ~eq;                  // 不等判断

wire a_neg = a[31];              // a的符号位
wire a_zero = (a == 32'b0);      // a是否为0

// 有符号比较
wire gt0 = (~a_neg) & (~a_zero); // a > 0
wire le0 = a_neg | a_zero;        // a <= 0
wire lt0 = a_neg;                 // a < 0
wire ge0 = ~a_neg;                // a >= 0

assign cmp_result =
    (cmp_op == 3'b000) ? eq  :   // BEQ
    (cmp_op == 3'b001) ? neq :   // BNE
    (cmp_op == 3'b010) ? gt0 :   // BGTZ
    (cmp_op == 3'b011) ? le0 :   // BLEZ
    (cmp_op == 3'b100) ? lt0 :   // BLTZ
    (cmp_op == 3'b101) ? ge0 :   // BGEZ
                         1'b0;
```

**关键特性**：
- 在ID阶段完成比较，减少分支延迟
- 支持全部6种分支条件
- 采用组合逻辑，零延迟输出

---

## 三、指令流转与各模块功能

### 3.1 模块架构图

```
┌─────────────────────────────────────────────────────────────┐
│                     MIPS 流水线 CPU                          │
│                                                              │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐ │
│  │   IF     │──▶│   ID     │──▶│   EX     │──▶│   MEM    │──▶│   WB     │
│  │  取指    │   │  译码    │   │  执行    │   │  访存    │   │  写回    │
│  └──────────┘   └──────────┘   └──────────┘   └──────────┘   └──────────┘
│                       │                                           │
│                       ▼                                           │
│               ┌──────────────┐                                   │
│               │  Branch_Cmp  │                                   │
│               │  分支比较器  │                                   │
│               └──────────────┘                                   │
│                       │                                           │
│                       ▼                                           │
│               ┌──────────────┐                                   │
│               │  controller2 │                                   │
│               │  控制器      │                                   │
│               │  maindec2    │                                   │
│               │  aludec      │                                   │
│               └──────────────┘                                   │
│                       │                                           │
│                       ▼                                           │
│               ┌──────────────┐                                   │
│               │   hazard     │◀──────────────────────────────────┘
│               │  冒险检测    │
│               └──────────────┘
└─────────────────────────────────────────────────────────────┘
```

### 3.2 各模块功能详解

#### 3.2.1 maindec2.v（主译码器）

**功能**：
1. 根据指令的**opcode**、**rt**字段进行译码
2. 生成19位控制信号，控制整个流水线的行为
3. 识别所有B类和J类指令

**输入**：
- `op[5:0]`：指令操作码
- `rt[4:0]`：rt字段（用于REGIMM指令区分）
- `funct[5:0]`：功能码（保留，JR/JALR在controller2中处理）

**输出控制信号**：
- `branch`：分支指令标志
- `jump`：跳转指令标志
- `branch_op[2:0]`：分支比较类型
- `link`：链接标志（保存返回地址）
- `regwrite`：寄存器写使能

#### 3.2.2 controller2.v（总控制器）

**功能**：
1. 调用`maindec2`进行主译码
2. 调用`aludec`进行ALU操作译码
3. 特殊处理JR/JALR指令
4. 生成各流水线阶段的控制信号
5. 管理控制信号在流水线中的传递

**关键逻辑**：
```verilog
// JR/JALR检测（在ID阶段）
wire is_jr   = (opD == 6'b000000) && (functD == 6'b001000);
wire is_jalr = (opD == 6'b000000) && (functD == 6'b001001);
assign jumpregD = is_jr | is_jalr;

// 分支跳转判断
assign pcsrcD = branchD & equalD;  // 分支且条件满足才跳转

// JR不写寄存器，JALR写寄存器
assign regwriteD = regwriteD_raw & ~is_jr;
```

**流水线控制信号传递**：
- ID阶段：`branchD`, `jumpD`, `jumpregD`, `branch_opD`
- EX阶段：`linkE`, `jalrE`（通过floprc传递）
- MEM阶段：`linkM`, `jalrM`
- WB阶段：`linkW`, `jalrW`

#### 3.2.3 datapath.v（数据通路）

**功能**：
1. 实现五级流水线的数据传递
2. 包含PC逻辑、分支目标计算、跳转目标计算
3. 实现返回地址计算（PC+8）
4. 集成Branch_Cmp进行分支判断
5. 处理延迟槽机制

**分支跳转相关关键逻辑**：

##### 分支目标地址计算（ID阶段）
```verilog
// 分支偏移左移2位
sl2 immsh(signimmD, signimmshD);

// 计算分支目标：PC+4 + (offset << 2)
adder pcadd2(pcplus4D, signimmshD, pcbranchD);
```

##### 跳转目标地址计算（ID阶段）
```verilog
// J/JAL的跳转地址：{PC+4[31:28], instr[25:0], 2'b00}
assign pcjumpD = {pcplus4D[31:28], instrD[25:0], 2'b00};

// JR/JALR的跳转地址：寄存器rs的值（前推后）
assign pcjumpregD = srca2D;
```

##### 返回地址计算（ID阶段）
```verilog
// 延迟槽架构：返回地址 = 跳转指令PC + 8
// pcplus4D = PC跳转 + 4，再加4得到PC+8
adder pcadd3(pcplus4D, 32'h4, pcplus8D);
```

##### PC选择逻辑（ID阶段）
```verilog
// 1. 分支/不分支选择
mux2 #(32) pcbrmux(pcplus4F, pcbranchD, pcsrcD, pcnextbrFD);

// 2. J/JAL vs JR/JALR选择
mux2 #(32) pcjumpmux(pcjumpD, pcjumpregD, jumpregD, pcnextbrjD);

// 3. 顺序执行 vs 跳转选择
mux2 #(32) pcfinalmux(pcnextbrFD, pcnextbrjD, jumpD | jumpregD, pcnextFD);
```

##### 分支比较（ID阶段）
```verilog
// 前推处理（从MEM阶段）
mux2 #(32) forwardamux(srcaD, aluoutM, forwardaD, srca2D);
mux2 #(32) forwardbmux(srcbD, aluoutM, forwardbD, srcb2D);

// 分支条件判断
Branch_Cmp branch_cmp(
    .a(srca2D), .b(srcb2D), 
    .cmp_op(branch_opD), 
    .cmp_result(equalD)
);
```

#### 3.2.4 hazard.v（冒险检测单元）

**功能**：
1. 检测数据冒险并插入停顿（stall）
2. 生成前推控制信号
3. 特别处理分支/跳转相关的冒险

**关键逻辑**：

##### ID阶段前推（用于分支/JR判断）
```verilog
// 从MEM阶段前推到ID阶段
assign forwardaD = (rsD != 0 & rsD == writeregM & regwriteM);
assign forwardbD = (rtD != 0 & rtD == writeregM & regwriteM);
```

##### 分支/跳转寄存器冲突停顿
```verilog
// 如果ID阶段是分支或JR/JALR，且前面的指令会修改所需的寄存器
assign branchstallD = (branchD | jumpregD) & 
                      ((regwriteE & (writeregE == rsD | writeregE == rtD)) |
                       (memtoregM & (writeregM == rsD | writeregM == rtD)));
```

**停顿场景说明**：
- **场景1**：前面是ALU指令（在EX阶段），分支需要等待ALU结果
- **场景2**：前面是Load指令（在MEM阶段），分支需要等待内存数据

##### 冲刷（Flush）逻辑
```verilog
// 延迟槽机制：只在停顿时冲刷EX级，插入气泡
assign flushE = stallD;
```

**关键特性**：
- **支持延迟槽**：正常跳转时不冲刷，延迟槽指令正常执行
- **停顿时插入气泡**：当冲突需要停顿时，在EX级插入NOP

---

## 四、指令执行流程

### 4.1 B类指令执行流程（以BEQ为例）

```
时钟周期：  T0        T1        T2        T3        T4        T5
         ┌─────────┬─────────┬─────────┬─────────┬─────────┐
IF       │ BEQ     │ delay   │ target/ │ ...     │         │
         │         │ slot    │ PC+8    │         │         │
         └─────────┴─────────┴─────────┴─────────┴─────────┘
                   ┌─────────┬─────────┬─────────┬─────────┐
ID                 │ BEQ     │ delay   │ target/ │ ...     │
                   │ 译码    │ slot    │ PC+8    │         │
                   │ 比较    │         │         │         │
                   └─────────┴─────────┴─────────┴─────────┘
                             ┌─────────┬─────────┬─────────┐
EX                           │ delay   │ target/ │ ...     │
                             │ slot    │ PC+8    │         │
                             └─────────┴─────────┴─────────┘
                                       ┌─────────┬─────────┐
MEM                                    │ target/ │ ...     │
                                       │ PC+8    │         │
                                       └─────────┴─────────┘
                                                 ┌─────────┐
WB                                               │ target/ │
                                                 │ PC+8    │
                                                 └─────────┘
```

**步骤说明**：
1. **T0 (IF)**：取BEQ指令
2. **T1 (ID)**：
   - 译码BEQ，识别为分支指令
   - 读取rs和rt寄存器
   - 通过Branch_Cmp比较rs和rt
   - 计算分支目标地址：PC+4+(offset<<2)
   - 确定是否跳转：pcsrcD = branchD & equalD
   - **同时取延迟槽指令**
3. **T2 (EX)**：延迟槽指令进入EX阶段执行
4. **T3 (MEM)**：根据T1的判断结果，取目标地址或PC+8的指令
5. **T4-T5**：继续执行

**关键点**：
- **延迟槽**：BEQ后的一条指令**总是执行**，无论分支是否跳转
- **分支判断提前**：在ID阶段完成，减少分支延迟到1个周期
- **无需冲刷**：延迟槽指令正常流过流水线

### 4.2 J类指令执行流程（以JAL为例）

```
时钟周期：  T0        T1        T2        T3        T4
         ┌─────────┬─────────┬─────────┬─────────┐
IF       │ JAL     │ delay   │ target  │ ...     │
         │ target  │ slot    │         │         │
         └─────────┴─────────┴─────────┴─────────┘
                   ┌─────────┬─────────┬─────────┐
ID                 │ JAL     │ delay   │ target  │
                   │ 译码    │ slot    │         │
                   │ 计算PC+8│         │         │
                   └─────────┴─────────┴─────────┘
                             ┌─────────┬─────────┐
EX                           │ delay   │ target  │
                             │ slot    │         │
                             │ PC+8→$31│         │
                             └─────────┴─────────┘
                                       ┌─────────┐
MEM                                    │ target  │
                                       │ (PC+8传递)│
                                       └─────────┘
                                                 ┌─────────┐
WB                                               │ target  │
                                                 │ $31=PC+8│
                                                 └─────────┘
```

**步骤说明**：
1. **T0 (IF)**：取JAL指令
2. **T1 (ID)**：
   - 译码JAL，识别为跳转链接指令
   - 计算跳转目标：{PC+4[31:28], instr[25:0], 2'b00}
   - 计算返回地址：PC+8（延迟槽架构）
   - 生成jumpD=1, linkD=1信号
   - **同时取延迟槽指令**
3. **T2 (EX)**：
   - 延迟槽指令进入EX阶段
   - JAL的linkE=1信号传递
   - PC+8通过流水线传递
4. **T3 (MEM)**：取目标地址指令，PC+8继续传递
5. **T4 (WB)**：PC+8写入$31寄存器

**关键点**：
- **返回地址**：保存JAL指令地址+8（跳过延迟槽）
- **目标寄存器**：固定为$31
- **延迟槽执行**：JAL后的一条指令总是执行

### 4.3 JR/JALR执行流程（以JALR为例）

```
时钟周期：  T0        T1        T2        T3        T4
         ┌─────────┬─────────┬─────────┬─────────┐
IF       │ JALR $31│ delay   │ target  │ ...     │
         │  $rs    │ slot    │ (rs值)  │         │
         └─────────┴─────────┴─────────┴─────────┘
                   ┌─────────┬─────────┬─────────┐
ID                 │ JALR    │ delay   │ target  │
                   │ 读$rs   │ slot    │         │
                   │ 计算PC+8│         │         │
                   │ 前推检测│         │         │
                   └─────────┴─────────┴─────────┘
                             ┌─────────┬─────────┐
EX                           │ delay   │ target  │
                             │ slot    │         │
                             │PC+8→$rd │         │
                             └─────────┴─────────┘
                                       ┌─────────┐
MEM                                    │ target  │
                                       │(PC+8传递)│
                                       └─────────┘
                                                 ┌─────────┐
WB                                               │ target  │
                                                 │$rd=PC+8 │
                                                 └─────────┘
```

**步骤说明**：
1. **T0 (IF)**：取JALR指令
2. **T1 (ID)**：
   - 译码识别JALR（opcode=0, funct=0x09）
   - 读取rs寄存器（跳转目标地址）
   - **冒险检测**：如果前面指令会修改rs，需要前推或停顿
   - 计算返回地址：PC+8
   - 生成jumpregD=1, jalrD=1信号
3. **T2 (EX)**：
   - 延迟槽指令执行
   - JALR的jalrE=1，PC+8传递
4. **T3 (MEM)**：取目标地址（rs寄存器值）的指令
5. **T4 (WB)**：PC+8写入rd寄存器（通常是$31）

**关键点**：
- **跳转目标**：由寄存器rs决定（动态跳转）
- **返回地址**：保存到rd寄存器（默认$31，可指定）
- **数据冒险**：需要检测rs的RAW冒险

### 4.4 带链接的条件分支（以BLTZAL为例）

```
时钟周期：  T0        T1        T2        T3        T4        T5
         ┌─────────┬─────────┬─────────┬─────────┬─────────┐
IF       │ BLTZAL  │ delay   │ target/ │ ...     │         │
         │   $rs   │ slot    │ PC+8    │         │         │
         └─────────┴─────────┴─────────┴─────────┴─────────┘
                   ┌─────────┬─────────┬─────────┬─────────┐
ID                 │ BLTZAL  │ delay   │ target/ │ ...     │
                   │ 读$rs   │ slot    │ PC+8    │         │
                   │ 判断<0  │         │         │         │
                   │ 计算PC+8│         │         │         │
                   └─────────┴─────────┴─────────┴─────────┘
                             ┌─────────┬─────────┬─────────┐
EX                           │ delay   │ target/ │ ...     │
                             │ slot    │ PC+8    │         │
                             │PC+8→$31 │         │         │
                             └─────────┴─────────┴─────────┘
                                       ┌─────────┬─────────┐
MEM                                    │ target/ │ ...     │
                                       │ PC+8    │         │
                                       │(PC+8传递)│         │
                                       └─────────┴─────────┘
                                                 ┌─────────┐
WB                                               │ target/ │
                                                 │$31=PC+8 │
                                                 └─────────┘
```

**步骤说明**：
1. **T0 (IF)**：取BLTZAL指令
2. **T1 (ID)**：
   - 译码识别BLTZAL（opcode=000001, rt=10000）
   - 读取rs寄存器
   - 判断rs < 0（通过Branch_Cmp）
   - 计算分支目标和返回地址PC+8
   - 生成branchD=1, linkD=1信号
3. **T2 (EX)**：
   - 延迟槽指令执行
   - linkE=1，PC+8传递
4. **T3 (MEM/WB)**：根据条件跳转或顺序执行，同时PC+8写入$31

**关键点**：
- **无条件写寄存器**：无论分支是否跳转，都保存返回地址到$31
- **条件跳转**：只有rs < 0时才跳转到目标地址
- **返回地址**：保存BLTZAL指令地址+8

---

## 五、冒险处理

### 5.1 数据冒险

#### 5.1.1 Load-Use冲突
```
LOAD  $2, 0($3)    # T0: IF, T1: ID, T2: EX, T3: MEM(数据可用), T4: WB
BEQ   $2, $4, LABEL # T1: IF, T2: ID(需要$2), ...
```

**解决方案**：
- hazard.v检测到load指令（memtoregE=1）的目标寄存器与分支的源寄存器冲突
- 生成lwstallD=1，停顿Fetch和Decode阶段1个周期
- 生成flushE=1，在Execute阶段插入气泡（NOP）
- 下一周期，load数据从MEM阶段前推到ID阶段

#### 5.1.2 Branch-ALU冲突
```
ADD  $2, $3, $4    # T0: IF, T1: ID, T2: EX(ALU计算), T3: MEM, T4: WB
BEQ  $2, $5, LABEL # T1: IF, T2: ID(需要$2), ...
```

**解决方案**：
- hazard.v检测到EX阶段指令会修改分支需要的寄存器
- 生成branchstallD=1，停顿1个周期
- 下一周期，ALU结果从MEM阶段前推到ID阶段

#### 5.1.3 JR-ALU冲突
```
ADD  $31, $2, $3   # 计算跳转地址
JR   $31           # 需要$31的值
```

**解决方案**：同分支冒险，停顿+前推

### 5.2 控制冒险

**延迟槽机制**：
- 分支/跳转指令后的一条指令（延迟槽）**总是执行**
- 分支判断在ID阶段完成，有效分支延迟为1个周期
- 编译器/程序员负责填充延迟槽（有用指令或NOP）

**优势**：
- 减少控制冒险带来的性能损失
- 无需分支预测和推测执行
- 流水线实现简单

---

## 六、总结

### 6.1 指令实现位置总结

| 指令类型 | 定义位置 | 判别位置 | 执行位置 | 关键模块 |
|---------|---------|---------|---------|---------|
| BEQ/BNE/BGTZ/BLEZ | maindec2.v | Branch_Cmp.v (ID) | datapath.v (ID) | Branch_Cmp, hazard |
| BLTZ/BGEZ/BLTZAL/BGEZAL | maindec2.v | Branch_Cmp.v (ID) | datapath.v (ID) | Branch_Cmp, hazard |
| J/JAL | maindec2.v | controller2.v (ID) | datapath.v (ID) | PC逻辑 |
| JR/JALR | controller2.v | controller2.v (ID) | datapath.v (ID) | PC逻辑, hazard |

### 6.2 模块功能总结

| 模块 | 主要功能 |
|------|---------|
| **maindec2.v** | 主译码器，根据opcode/rt生成控制信号 |
| **controller2.v** | 总控制器，处理JR/JALR，管理流水线控制信号 |
| **Branch_Cmp.v** | 分支比较器，在ID阶段判断分支条件 |
| **datapath.v** | 数据通路，实现五级流水线，包含PC逻辑、寄存器文件、ALU等 |
| **hazard.v** | 冒险检测单元，处理数据冒险和控制冒险 |

### 6.3 设计特点

1. **提前分支判断**：分支条件在ID阶段计算，减少分支延迟
2. **延迟槽机制**：支持MIPS标准延迟槽，提高流水线效率
3. **完整的冒险处理**：通过前推和停顿解决数据冒险
4. **统一的控制流**：所有分支跳转指令统一在ID阶段处理
5. **精确异常支持**：配合异常处理机制，保证精确异常

---

**适用场景**：本文档适用于本科生毕业设计答辩，清晰展示MIPS流水线CPU中分支跳转指令的实现原理和工程实践。
