# MIPS五级流水线详细说明

## 一、概述

本MIPS CPU采用经典的五级流水线结构，支持延迟槽机制，实现了完整的MIPS-I指令集及部分MIPS-II/III扩展指令。

**五级流水线阶段**：
1. **IF (Instruction Fetch)** - 取指阶段
2. **ID (Instruction Decode)** - 译码阶段
3. **EX (Execute)** - 执行阶段
4. **MEM (Memory Access)** - 访存阶段
5. **WB (Write Back)** - 写回阶段

---

## 二、各阶段详细说明

### 2.1 IF阶段（取指阶段，Instruction Fetch）

#### 2.1.1 功能说明
- **主要任务**：根据PC（程序计数器）从指令存储器中取出指令
- **次要任务**：计算下一条指令地址（PC+4）

#### 2.1.2 实现文件/模块
- **主模块**：`datapath.v` (行152-157)
- **子模块**：
  - `pc.v`：程序计数器寄存器
  - `adder.v`：加法器（计算PC+4）

#### 2.1.3 关键数据流

```verilog
// PC寄存器（datapath.v, 行156）
pc #(.WIDTH(32), .RESET_VALUE(32'hBFC0_0000)) 
   pcreg(clk, rst, (~stallF) | flush_except, pcnextF, pcF);

// PC+4计算（datapath.v, 行157）
adder pcadd1(pcF, 32'h4, pcplus4F);
```

**数据流向**：
```
pcF (当前PC)
  │
  ├──▶ 指令存储器 ──▶ instrF (取出的指令)
  │
  └──▶ 加法器(+4) ──▶ pcplus4F (PC+4)
```

#### 2.1.4 控制信号

| 信号 | 来源 | 功能 |
|------|------|------|
| `stallF` | hazard.v | 停顿IF阶段（冒险时） |
| `flush_except` | 异常逻辑 | 异常时重定向PC |
| `pcnextF` | ID阶段 | 下一条指令地址 |

**PC选择逻辑**（datapath.v, 行153-155）：
```verilog
wire [31:0] pcnextF = exc_commitM ? EXC_VECTOR :      // 异常入口
                      eret_commitM ? cp0_epc :         // ERET返回
                      pcnextFD;                        // 正常/分支/跳转
```

#### 2.1.5 针对B/J类指令的IF阶段
- **正常取指**：取分支/跳转指令本身
- **延迟槽**：下一周期取延迟槽指令（总是执行）
- **目标取指**：再下一周期取目标地址或PC+8的指令

**时序示例（BEQ跳转）**：
```
周期  | T0          | T1          | T2          | T3
IF    | BEQ指令     | 延迟槽指令  | 目标地址指令 | ...
PC    | PC_beq      | PC_beq+4    | PC_target   | ...
```

---

### 2.2 ID阶段（译码阶段，Instruction Decode）

#### 2.2.1 功能说明
- **主要任务**：
  1. 指令译码，生成控制信号
  2. 读取寄存器操作数
  3. **分支判断**（提前完成，减少分支延迟）
  4. 计算分支/跳转目标地址
  5. 确定下一条指令地址（PC选择）
- **次要任务**：
  1. 立即数扩展
  2. 计算返回地址（PC+8）
  3. 异常检测（RI、Sys、Break、Int等）

#### 2.2.2 实现文件/模块

##### 主要文件
- **datapath.v** (行159-353)：ID阶段数据通路
- **controller2.v**：控制器，调用译码模块
- **maindec2.v**：主译码器
- **aludec.v**：ALU译码器
- **Branch_Cmp.v**：分支比较器

##### 流水线寄存器
```verilog
// IF/ID流水线寄存器（datapath.v, 行160-163）
flopenr #(32) r1D(clk, rst, ~stallD, pcplus4F, pcplus4D);
flopenrc #(32) r2D(clk, rst, ~stallD, flushD, instrF, instrD);
```

#### 2.2.3 关键数据流

##### 指令字段提取（datapath.v, 行165-170）
```verilog
assign opD    = instrD[31:26];  // 操作码
assign functD = instrD[5:0];    // 功能码
assign rsD    = instrD[25:21];  // 源寄存器1
assign rtD    = instrD[20:16];  // 源寄存器2/目标寄存器
assign rdD    = instrD[15:11];  // 目标寄存器
assign saD    = instrD[10:6];   // 移位量
```

##### 寄存器读取（datapath.v, 行345）
```verilog
regfile rf(clk, regwriteW_eff, rsD, rtD, writeregW, resultW, 
           srcaD, srcbD);
```
- `srcaD`：rs寄存器的值
- `srcbD`：rt寄存器的值
- 从WB阶段写入，实现寄存器更新

##### 立即数扩展（datapath.v, 行289-296, 341-342）
```verilog
// 符号扩展/零扩展（ExtUnit.v）
wire [1:0] ext_typeD;
assign ext_typeD = (opD == 6'b001100 || opD == 6'b001101 || opD == 6'b001110) 
                   ? 2'b01 : 2'b00;  // ANDI/ORI/XORI零扩展，其他符号扩展
ExtUnit extu(instrD[15:0], ext_typeD, signimmD);

// LUI立即数（左移16位）
assign luiimmD = {instrD[15:0], 16'b0};

// 零扩展立即数
assign zeroimmD = {16'b0, instrD[15:0]};
```

#### 2.2.4 控制信号生成

##### 主译码器（maindec2.v）
```verilog
maindec2 md(
    .op(opD), .rt(rtD), .funct(functD),
    .regwrite(regwriteD_raw),
    .regdst(regdstD),
    .alusrc(alusrcD),
    .branch(branchD),
    .memwrite(memwriteD),
    .memtoreg(memtoregD),
    .jump(jumpD),
    .aluop(aluopD),
    .hassign(hassign_md),
    .islui(isluiD),
    .mem_op(mem_opD),
    .branch_op(branch_opD),
    .link(linkD)
);
```

##### ALU译码器（aludec.v）
```verilog
aludec ad(rst, functD, aluopD, 
          alucontrolD, hassign_ad, 
          hilo_enD, hilo_mfD, divD, shiftD);
```

##### 主要控制信号

| 信号 | 含义 | 用途 |
|------|------|------|
| `regwriteD` | 寄存器写使能 | 标识指令是否写寄存器 |
| `regdstD` | 写寄存器选择 | 0:rt, 1:rd |
| `alusrcD` | ALU第二操作数选择 | 0:寄存器, 1:立即数 |
| `branchD` | 分支指令标志 | 标识是否为分支指令 |
| `jumpD` | 跳转指令标志 | 标识是否为J/JAL |
| `jumpregD` | 寄存器跳转标志 | 标识是否为JR/JALR |
| `memwriteD` | 内存写使能 | Store指令 |
| `memtoregD` | 写回数据选择 | 0:ALU结果, 1:内存数据 |
| `aluopD[2:0]` | ALU操作类型 | 传递给ALU译码器 |
| `branch_opD[2:0]` | 分支比较类型 | 传递给Branch_Cmp |
| `linkD` | 链接标志 | JAL/JALR/BLTZAL/BGEZAL |

#### 2.2.5 分支判断逻辑（关键）

##### 数据前推（从MEM阶段）
```verilog
// 前推逻辑（datapath.v, 行327-328）
mux2 #(32) forwardamux(srcaD, aluoutM, forwardaD, srca2D);
mux2 #(32) forwardbmux(srcbD, aluoutM, forwardbD, srcb2D);
```
- 如果MEM阶段指令写的寄存器与当前分支读的寄存器相同，前推MEM阶段的ALU结果

##### 分支比较（Branch_Cmp.v）
```verilog
// 分支条件判断（datapath.v, 行330-332）
Branch_Cmp branch_cmp(
    .a(srca2D),           // 前推后的rs值
    .b(srcb2D),           // 前推后的rt值
    .cmp_op(branch_opD),  // 比较类型（BEQ/BNE/BGTZ等）
    .cmp_result(equalD)   // 比较结果
);
```

**branch_opD编码**：
- `000`: BEQ (a == b)
- `001`: BNE (a != b)
- `010`: BGTZ (a > 0)
- `011`: BLEZ (a <= 0)
- `100`: BLTZ (a < 0)
- `101`: BGEZ (a >= 0)

##### 分支跳转决策（controller2.v, 行91）
```verilog
assign pcsrcD = branchD & equalD;  // 分支指令且条件满足才跳转
```

#### 2.2.6 地址计算

##### 分支目标地址
```verilog
// 立即数左移2位（datapath.v, 行320）
sl2 immsh(signimmD, signimmshD);

// 分支目标 = PC+4 + (offset << 2)（datapath.v, 行321）
adder pcadd2(pcplus4D, signimmshD, pcbranchD);
```

##### 跳转目标地址
```verilog
// J/JAL跳转地址（datapath.v, 行334）
assign pcjumpD = {pcplus4D[31:28], instrD[25:0], 2'b00};

// JR/JALR跳转地址（寄存器值，datapath.v, 行335）
assign pcjumpregD = srca2D;
```

##### 返回地址（链接指令）
```verilog
// 返回地址 = 当前指令PC + 8（延迟槽架构）（datapath.v, 行325）
adder pcadd3(pcplus4D, 32'h4, pcplus8D);
```
- `pcplus4D`：当前指令PC+4
- `pcplus8D`：PC+8，跳过延迟槽指令

#### 2.2.7 PC选择逻辑
```verilog
// 1. 分支选择（datapath.v, 行337）
mux2 #(32) pcbrmux(pcplus4F, pcbranchD, pcsrcD, pcnextbrFD);
// pcsrcD=0: 顺序执行(PC+4)
// pcsrcD=1: 跳转到分支目标

// 2. 跳转类型选择（datapath.v, 行338）
mux2 #(32) pcjumpmux(pcjumpD, pcjumpregD, jumpregD, pcnextbrjD);
// jumpregD=0: J/JAL跳转(26位地址)
// jumpregD=1: JR/JALR跳转(寄存器地址)

// 3. 最终PC选择（datapath.v, 行339）
mux2 #(32) pcfinalmux(pcnextbrFD, pcnextbrjD, jumpD | jumpregD, pcnextFD);
// (jumpD | jumpregD)=0: 正常执行或分支
// (jumpD | jumpregD)=1: 跳转
```

#### 2.2.8 针对B/J类指令的ID阶段

##### BEQ指令（branch_opD=000）
```
控制信号：branchD=1, branch_opD=000, pcsrcD=(rs==rt)
数据流：
  srcaD (rs) ──┐
               ├─▶ Branch_Cmp ──▶ equalD ──▶ pcsrcD
  srcbD (rt) ──┘                              │
                                              ▼
  signimmD ──▶ sl2 ──▶ signimmshD ──┐     PC选择
  pcplus4D ────────────────────────┘
           ──▶ adder ──▶ pcbranchD
```

##### JAL指令
```
控制信号：jumpD=1, linkD=1, regwriteD=1
数据流：
  instrD[25:0] ──┐
                 ├─▶ {PC[31:28], instr[25:0], 2'b00} ──▶ pcjumpD
  pcplus4D[31:28]┘                                        │
                                                          ▼
  pcplus4D ──▶ +4 ──▶ pcplus8D ──▶ [流水线传递] ──▶ $31
```

##### JR指令
```
控制信号：jumpregD=1, regwriteD=0
数据流：
  srcaD (rs) ──▶ [前推] ──▶ srca2D ──▶ pcjumpregD ──▶ PC
```

##### JALR指令
```
控制信号：jumpregD=1, jalrD=1, regwriteD=1
数据流：
  srcaD (rs) ──▶ [前推] ──▶ srca2D ──▶ pcjumpregD ──▶ PC
  pcplus4D ──▶ +4 ──▶ pcplus8D ──▶ [流水线传递] ──▶ rd
```

##### BLTZAL指令（branch_opD=100, linkD=1）
```
控制信号：branchD=1, branch_opD=100, linkD=1, regwriteD=1
数据流：
  srcaD (rs) ──▶ Branch_Cmp ──▶ equalD (rs<0) ──▶ pcsrcD
  pcplus4D ──▶ +offset ──▶ pcbranchD
  pcplus4D ──▶ +4 ──▶ pcplus8D ──▶ [流水线传递] ──▶ $31
```

---

### 2.3 EX阶段（执行阶段，Execute）

#### 2.3.1 功能说明
- **主要任务**：
  1. ALU运算（算术、逻辑、移位等）
  2. 计算内存访问地址（Load/Store）
  3. HI/LO寄存器操作（乘除法）
  4. 选择写回寄存器编号
- **次要任务**：
  1. 检测溢出异常
  2. 传递链接返回地址

#### 2.3.2 实现文件/模块

##### 主要文件
- **datapath.v** (行355-436)：EX阶段数据通路
- **alu.v**：算术逻辑单元
- **divider.v**：除法器
- **hilo_reg.v**：HI/LO寄存器

##### 流水线寄存器
```verilog
// ID/EX流水线寄存器（datapath.v, 行372-382）
floprc #(32) r1E(clk, rst, flushE_eff, srcaD, srcaE);
floprc #(32) r2E(clk, rst, flushE_eff, srcbD, srcbE);
floprc #(32) r3E(clk, rst, flushE_eff, signimmD, signimmE);
floprc #(5)  r4E(clk, rst, flushE_eff, rsD, rsE);
floprc #(5)  r5E(clk, rst, flushE_eff, rtD, rtE);
floprc #(5)  r6E(clk, rst, flushE_eff, rdD, rdE);
floprc #(32) r10E(clk, rst, flushE_eff, pcplus8D, pcplus8E);
// ... 还有其他控制信号和数据
```

#### 2.3.3 关键数据流

##### 数据前推（从MEM和WB阶段）
```verilog
// ALU操作数前推（datapath.v, 行384-385）
mux3 #(32) forwardaemux(srcaE, resultW, aluoutM, forwardaE, srca2E);
mux3 #(32) forwardbemux(srcbE, resultW, aluoutM, forwardbE, srcb2E);
```

**forwardaE编码**：
- `00`: 使用EX阶段原始数据（srcaE）
- `01`: 前推WB阶段数据（resultW）
- `10`: 前推MEM阶段数据（aluoutM）

##### ALU操作数选择
```verilog
// 立即数选择（datapath.v, 行387）
mux3 #(32) srcextmux(signimmE, zeroimmE, luiimmE, 
                     {isluiE, alucontrolE[0]}, srcimmE);

// 第一操作数选择（移位指令使用sa）（datapath.v, 行388）
mux2 #(32) srcamux(srca2E, {27'b0, saE}, shiftE[0], srca3E);

// 第二操作数选择（寄存器或立即数）（datapath.v, 行389）
mux2 #(32) srcbmux(srcb2E, srcimmE, alusrcE, srcb3E);
```

##### ALU运算
```verilog
// ALU（datapath.v, 行391）
alu alu(srca3E, srcb3E, alucontrolE, hassignE, shiftE[1], 
        aluresultE, aluresult_loE, overflowE, zeroE);
```

**alucontrolE操作**：
- `000`: SLT（有符号比较）
- `001`: OR / SLL
- `010`: ADD / ADDU
- `011`: AND / SRL
- `100`: MULT / MULTU
- `101`: NOR / SRA
- `110`: SUB / SUBU
- `111`: XOR

##### 写回寄存器选择
```verilog
// 寄存器号选择（datapath.v, 行417-421）
// 优先级：linkE($31) > jalrE(rd) > regdstE(rd/rt)
mux3 #(5) wrmux(rtE, rdE, 5'd31, 
                {linkE, (regdstE & ~linkE)}, 
                writeregE_temp);
```

**选择逻辑**：
- JAL/BLTZAL/BGEZAL（linkE=1）：写$31
- JALR（jalrE=1）：写rd
- R型指令（regdstE=1）：写rd
- I型指令（regdstE=0）：写rt

##### HI/LO寄存器操作
```verilog
// HI/LO写入（datapath.v, 行438-447）
hilo_reg hilo(
    .clk(clk), .rst(rst),
    .we(hilo_en_gated),       // 写使能（异常时禁止）
    .hi_in(hi_inE), .lo_in(lo_inE),
    .hi_out(hi_outE), .lo_out(lo_outE)
);
```

**hilo_enE编码**：
- `00`: 不写
- `01`: 写HI和LO（MULT/MULTU/DIV/DIVU）
- `10`: 仅写LO（MTLO）
- `11`: 仅写HI（MTHI）

**hilo_mfE编码**：
- `00`: 读LO（MFLO）
- `01`: 读HI（MFHI）
- `10`: 不读

#### 2.3.4 控制信号

| 信号 | 来源 | 功能 |
|------|------|------|
| `alusrcE` | controller2 | ALU第二操作数选择 |
| `alucontrolE[2:0]` | controller2 | ALU操作类型 |
| `regdstE` | controller2 | 写寄存器选择 |
| `regwriteE` | controller2 | 寄存器写使能 |
| `linkE` | controller2 | 链接指令标志 |
| `jalrE` | controller2 | JALR指令标志 |
| `forwardaE[1:0]` | hazard | 第一操作数前推控制 |
| `forwardbE[1:0]` | hazard | 第二操作数前推控制 |
| `flushE` | hazard | 冲刷EX阶段（插入气泡） |

#### 2.3.5 针对B/J类指令的EX阶段

##### 普通分支指令（BEQ/BNE等）
- **EX阶段**：执行延迟槽指令（可能是任意指令）
- **控制信号**：延迟槽指令的正常控制信号
- **数据流**：延迟槽指令的正常数据流
- **分支指令本身**：已在ID阶段完成，不进入EX阶段

##### JAL指令
```
控制信号：linkE=1, regwriteE=1, writeregE=$31
数据流：
  pcplus8E ──▶ [传递到MEM阶段] ──▶ [传递到WB阶段] ──▶ $31
```
- **EX阶段**：执行延迟槽指令
- **JAL本身**：pcplus8传递，准备写回$31

##### JALR指令
```
控制信号：jalrE=1, regwriteE=1, writeregE=rd
数据流：
  pcplus8E ──▶ [传递到MEM阶段] ──▶ [传递到WB阶段] ──▶ rd
```

##### BLTZAL指令
```
控制信号：linkE=1, regwriteE=1, writeregE=$31
数据流：
  pcplus8E ──▶ [传递到MEM阶段] ──▶ [传递到WB阶段] ──▶ $31
```
- **无论分支是否跳转**，返回地址都写入$31

---

### 2.4 MEM阶段（访存阶段，Memory Access）

#### 2.4.1 功能说明
- **主要任务**：
  1. 访问数据存储器（Load/Store指令）
  2. 检测内存访问异常（AdEL/AdES）
  3. 传递ALU结果和写回数据
- **次要任务**：
  1. 异常提交（精确异常）
  2. 传递链接返回地址

#### 2.4.2 实现文件/模块

##### 主要文件
- **datapath.v** (行449-479)：MEM阶段数据通路
- **DM.v**：数据存储器（外部模块，未包含在datapath中）
- **CP0_Reg.v**：CP0协处理器寄存器

##### 流水线寄存器
```verilog
// EX/MEM流水线寄存器（datapath.v, 行449-461）
floprc #(32) r1M(clk, rst, flushM, srcb2E, writedataM_temp);
floprc #(32) r2M(clk, rst, flushM, aluresultE, aluoutM_temp);
floprc #(5)  r3M(clk, rst, flushM, writeregE, writeregM);
floprc #(32) r4M(clk, rst, flushM, pcplus8E, pcplus8M);
// ... 还有其他控制信号
```

#### 2.4.3 关键数据流

##### Load指令数据流
```
aluoutM (地址) ──▶ 数据存储器 ──▶ readdataM ──▶ [WB阶段]
```

##### Store指令数据流
```
aluoutM (地址) ──┐
                 ├─▶ 数据存储器
writedataM (数据)┘
```

**内存操作类型**（mem_opM[2:0]）：
- `000`: 字访问（LW/SW，4字节）
- `001`: 半字存储（SH，2字节）
- `010`: 字节存储（SB，1字节）
- `100`: 半字加载（LH，符号扩展）
- `101`: 半字加载无符号（LHU，零扩展）
- `110`: 字节加载（LB，符号扩展）
- `111`: 字节加载无符号（LBU，零扩展）

##### 异常检测
```verilog
// 内存访问地址异常检测（datapath.v, 行464-482）
// AdEL (Load地址错误)：地址未对齐
// AdES (Store地址错误)：地址未对齐
```

**对齐要求**：
- LW/SW：地址必须4字节对齐（addr[1:0] == 2'b00）
- LH/LHU/SH：地址必须2字节对齐（addr[0] == 1'b0）
- LB/LBU/SB：无对齐要求

#### 2.4.4 控制信号

| 信号 | 来源 | 功能 |
|------|------|------|
| `memwriteM` | controller2 | 内存写使能 |
| `memtoregM` | controller2 | 写回数据选择（ALU或内存） |
| `regwriteM` | controller2 | 寄存器写使能 |
| `mem_opM[2:0]` | controller2 | 内存操作类型 |
| `linkM` | controller2 | 链接指令标志 |
| `jalrM` | controller2 | JALR指令标志 |

#### 2.4.5 异常提交逻辑

```verilog
// 异常提交（datapath.v, 行507-538）
assign exc_commitM = exc_validM & ~squashM_r;
assign eret_commitM = eretM & ~squashM_r;
```

**异常/ERET提交时**：
- 更新CP0寄存器（EPC、Cause、Status等）
- 生成异常向量地址或ERET返回地址
- 冲刷流水线（杀死年轻指令）

#### 2.4.6 针对B/J类指令的MEM阶段

##### 普通分支指令
- **MEM阶段**：执行延迟槽指令的内存操作（如果有）
- **分支指令本身**：不进入MEM阶段

##### JAL/JALR/BLTZAL/BGEZAL指令
```
控制信号：linkM=1, regwriteM=1
数据流：
  pcplus8M ──▶ [传递到WB阶段] ──▶ 写入寄存器
```

**关键点**：
- 链接指令的返回地址（PC+8）在MEM阶段传递
- 即使MEM阶段有内存操作（来自延迟槽指令），也不影响返回地址传递

---

### 2.5 WB阶段（写回阶段，Write Back）

#### 2.5.1 功能说明
- **主要任务**：
  1. 选择写回数据（ALU结果、内存数据、CP0数据、PC+8等）
  2. 写入寄存器文件
- **次要任务**：
  1. 生成调试信息（debug_wb_*）

#### 2.5.2 实现文件/模块

##### 主要文件
- **datapath.v** (行541-584)：WB阶段数据通路
- **regfile.v**：寄存器文件（在ID阶段实例化，但写操作在WB阶段）

##### 流水线寄存器
```verilog
// MEM/WB流水线寄存器（datapath.v, 行541-547）
flopr #(32) r1W(clk, rst, aluoutM, aluoutW);
flopr #(32) r2W(clk, rst, readdataM, readdataW);
flopr #(5)  r3W(clk, rst, writeregM, writeregW);
flopr #(32) r4W(clk, rst, pcplus8M, pcplus8W);
// ... 还有其他控制信号
```

#### 2.5.3 关键数据流

##### 写回数据选择
```verilog
// 写回数据选择（datapath.v, 行549-563）
wire [31:0] resultW_link;
wire [31:0] resultW_base;

// 1. 基础选择：ALU结果 vs 内存数据 vs CP0数据
mux3 #(32) resmux_base(
    aluoutW,       // ALU结果
    readdataW,     // 内存数据
    cp0_rdataW,    // CP0数据
    {mfc0W, (memtoregW & ~mfc0W)}, 
    resultW_base
);

// 2. 链接指令选择：普通结果 vs PC+8
mux2 #(32) resmux_link(
    resultW_base,  // 普通结果
    pcplus8W,      // 返回地址（PC+8）
    linkW,         // 链接指令标志
    resultW_link
);

// 3. JALR特殊处理（rd可能与普通指令冲突）
mux2 #(32) resmux(
    resultW_link,  // 链接或普通结果
    pcplus8W,      // JALR返回地址
    jalrW,         // JALR标志
    resultW
);
```

**选择优先级**：
1. **JALR**（jalrW=1）：写PC+8到rd
2. **链接指令**（linkW=1）：写PC+8到$31
3. **MFC0**（mfc0W=1）：写CP0数据
4. **Load**（memtoregW=1）：写内存数据
5. **默认**：写ALU结果

##### 寄存器写入
```verilog
// 寄存器文件（在datapath.v的ID阶段，行345）
regfile rf(
    .clk(clk),
    .we(regwriteW_eff),      // 写使能（异常时禁止）
    .ra1(rsD), .ra2(rtD),    // 读地址（ID阶段）
    .wa(writeregW),          // 写地址（WB阶段）
    .wd(resultW),            // 写数据（WB阶段）
    .rd1(srcaD), .rd2(srcbD) // 读数据（ID阶段）
);
```

**写使能条件**（datapath.v, 行125）：
```verilog
wire regwriteW_eff = (regwriteW | mfc0W) & ~exc_commitW & ~squashW_r;
```
- 正常写：regwriteW=1
- MFC0写：mfc0W=1
- 异常时禁止：exc_commitW=1时不写
- 被冲刷时禁止：squashW_r=1时不写

#### 2.5.4 控制信号

| 信号 | 来源 | 功能 |
|------|------|------|
| `regwriteW` | controller2 | 寄存器写使能 |
| `memtoregW` | controller2 | 写回数据选择 |
| `linkW` | controller2 | 链接指令标志 |
| `jalrW` | controller2 | JALR指令标志 |
| `mfc0W` | datapath | MFC0指令标志 |

#### 2.5.5 调试信息输出

```verilog
// 调试信息（datapath.v, 行568-584）
assign debug_wb_pc = pcW;
assign debug_wb_rf_wen = {4{regwriteW_eff}};  // 4位写使能
assign debug_wb_rf_wnum = writeregW;
assign debug_wb_rf_wdata = resultW;
```

#### 2.5.6 针对B/J类指令的WB阶段

##### 普通分支指令（BEQ/BNE等）
- **WB阶段**：执行延迟槽指令的写回
- **分支指令本身**：不写寄存器（regwriteW=0）

##### JAL指令
```
控制信号：linkW=1, regwriteW=1, writeregW=$31
数据流：
  pcplus8W ──▶ resultW ──▶ regfile[$31]
```
**写回内容**：JAL指令地址+8（跳过延迟槽）

##### JALR指令
```
控制信号：jalrW=1, regwriteW=1, writeregW=rd
数据流：
  pcplus8W ──▶ resultW ──▶ regfile[rd]
```
**写回内容**：JALR指令地址+8

##### BLTZAL/BGEZAL指令
```
控制信号：linkW=1, regwriteW=1, writeregW=$31
数据流：
  pcplus8W ──▶ resultW ──▶ regfile[$31]
```
**关键点**：无论分支是否跳转，都写入返回地址

---

## 三、冒险处理机制

### 3.1 数据冒险

#### 3.1.1 前推（Forwarding）

##### EX阶段前推（hazard.v, 行59-70）
```verilog
always @(*) begin
    forwardaE = 2'b00;
    forwardbE = 2'b00;
    // 从MEM阶段前推
    if(rsE != 0) begin
        if(rsE == writeregM & regwriteM)      forwardaE = 2'b10;
        else if(rsE == writeregW & regwriteW) forwardaE = 2'b01;
    end
    // 从WB阶段前推
    if(rtE != 0) begin
        if(rtE == writeregM & regwriteM)      forwardbE = 2'b10;
        else if(rtE == writeregW & regwriteW) forwardbE = 2'b01;
    end
end
```

**前推优先级**：MEM阶段 > WB阶段

##### ID阶段前推（hazard.v, 行55-56）
```verilog
assign forwardaD = (rsD != 0 & rsD == writeregM & regwriteM);
assign forwardbD = (rtD != 0 & rtD == writeregM & regwriteM);
```
- 仅从MEM阶段前推
- 用于分支/跳转寄存器判断

#### 3.1.2 停顿（Stall）

##### Load-Use冲突（hazard.v, 行75）
```verilog
assign lwstallD = memtoregE & (rtE == rsD | rtE == rtD);
```
**场景**：
```
LW   $2, 0($3)    # EX阶段，数据还未从内存读出
ADD  $4, $2, $5   # ID阶段，需要$2的值
```
**解决**：停顿1个周期，等待Load数据到达MEM阶段后前推

##### 分支/跳转寄存器冲突（hazard.v, 行79-81）
```verilog
assign branchstallD = (branchD | jumpregD) & 
                      ((regwriteE & (writeregE == rsD | writeregE == rtD)) |
                       (memtoregM & (writeregM == rsD | writeregM == rtD)));
```
**场景1**：
```
ADD  $2, $3, $4   # EX阶段，ALU正在计算
BEQ  $2, $5, L    # ID阶段，需要$2的值
```
**解决**：停顿1个周期，等待ALU结果到MEM阶段后前推

**场景2**：
```
LW   $2, 0($3)    # MEM阶段，数据刚从内存读出
BEQ  $2, $5, L    # ID阶段，需要$2的值
```
**解决**：停顿1个周期，等待Load数据到WB阶段（无法前推到ID）

### 3.2 控制冒险

#### 3.2.1 延迟槽机制
- **分支/跳转指令后的一条指令总是执行**
- 编译器负责填充有用指令或NOP
- 减少分支/跳转惩罚到1个周期

#### 3.2.2 提前分支判断
- 分支条件在ID阶段计算
- 分支目标在ID阶段确定
- 有效分支延迟：1个周期（延迟槽）

#### 3.2.3 流水线冲刷
```verilog
// 延迟槽架构下的冲刷逻辑（hazard.v, 行103）
assign flushE = stallD;
```
- **正常跳转**：不冲刷，延迟槽指令正常执行
- **停顿时**：冲刷EX阶段，插入气泡（NOP）

---

## 四、流水线时序图示例

### 4.1 无冒险顺序执行
```
指令序列：I1 → I2 → I3 → I4 → I5

周期  | T0  | T1  | T2  | T3  | T4  | T5  | T6  | T7  | T8
----- |-----|-----|-----|-----|-----|-----|-----|-----|-----
I1    | IF  | ID  | EX  | MEM | WB  |     |     |     |
I2    |     | IF  | ID  | EX  | MEM | WB  |     |     |
I3    |     |     | IF  | ID  | EX  | MEM | WB  |     |
I4    |     |     |     | IF  | ID  | EX  | MEM | WB  |
I5    |     |     |     |     | IF  | ID  | EX  | MEM | WB
```

### 4.2 BEQ分支（跳转）
```
指令序列：
  I1: BEQ  $2, $3, L     # 假设条件满足，跳转
  I2: ADD  $4, $5, $6    # 延迟槽
  L:  SUB  $7, $8, $9    # 目标指令

周期  | T0  | T1  | T2  | T3  | T4  | T5  | T6
----- |-----|-----|-----|-----|-----|-----|-----
BEQ   | IF  | ID  |     |     |     |     |
      |     | 比较|     |     |     |     |
      |     | 计算|     |     |     |     |
ADD   |     | IF  | ID  | EX  | MEM | WB  |
SUB   |     |     | IF  | ID  | EX  | MEM | WB
```
**说明**：
- T1：BEQ在ID阶段完成比较和目标计算，确定跳转
- T1：同时取延迟槽指令ADD
- T2：取目标指令SUB
- ADD总是执行（延迟槽）

### 4.3 JAL跳转
```
指令序列：
  I1: JAL  FUNC          # 跳转到FUNC
  I2: ADD  $2, $3, $4    # 延迟槽
  FUNC: SUB $5, $6, $7   # 目标函数

周期  | T0  | T1  | T2  | T3  | T4  | T5  | T6  | T7
----- |-----|-----|-----|-----|-----|-----|-----|-----
JAL   | IF  | ID  |     |     |     |     |     |
      |     | PC+8|     |     |     |     |     |
ADD   |     | IF  | ID  | EX  | MEM | WB  |     |
SUB   |     |     | IF  | ID  | EX  | MEM | WB  |
$31←  |     |     |     |     |     |     | WB  |
```
**说明**：
- T1：JAL在ID阶段计算PC+8和跳转目标
- T2-T6：延迟槽指令ADD正常执行
- T7：PC+8写入$31

### 4.4 Load-Use冒险（停顿）
```
指令序列：
  I1: LW   $2, 0($3)     # Load
  I2: ADD  $4, $2, $5    # 使用$2

周期  | T0  | T1  | T2  | T3  | T4  | T5  | T6
----- |-----|-----|-----|-----|-----|-----|-----
LW    | IF  | ID  | EX  | MEM | WB  |     |
ADD   |     | IF  | ID  | ID  | EX  | MEM | WB
      |     |     |停顿 | NOP |     |     |
      |     |     |     | 前推|     |     |
```
**说明**：
- T2：检测到Load-Use冒险，停顿ADD的ID阶段
- T3：在EX阶段插入气泡（NOP），LW进入MEM阶段
- T3：LW的数据从MEM阶段前推到ADD的ID阶段
- T4：ADD进入EX阶段，使用前推的数据

### 4.5 分支-ALU冒险（停顿）
```
指令序列：
  I1: ADD  $2, $3, $4    # ALU指令
  I2: BEQ  $2, $5, L     # 分支需要$2

周期  | T0  | T1  | T2  | T3  | T4  | T5  | T6
----- |-----|-----|-----|-----|-----|-----|-----
ADD   | IF  | ID  | EX  | MEM | WB  |     |
BEQ   |     | IF  | ID  | ID  |     |     |
      |     |     |停顿 | NOP |     |     |
      |     |     |     | 前推|     |     |
      |     |     |     | 比较|     |     |
延迟槽|     |     |     | IF  | ID  | EX  | MEM
```
**说明**：
- T2：检测到分支-ALU冒险，停顿BEQ的ID阶段
- T3：在EX阶段插入气泡，ADD进入MEM阶段
- T3：ADD的结果从MEM阶段前推到BEQ的ID阶段
- T3：BEQ完成比较和目标计算
- T3：取延迟槽指令

---

## 五、总结

### 5.1 五级流水线功能总结

| 阶段 | 英文名 | 主要功能 | 关键模块 |
|------|--------|----------|----------|
| **IF** | Instruction Fetch | 取指令，计算PC+4 | pc.v, adder.v |
| **ID** | Instruction Decode | 译码、读寄存器、分支判断、地址计算 | maindec2.v, Branch_Cmp.v, regfile.v |
| **EX** | Execute | ALU运算、前推、HI/LO操作 | alu.v, divider.v, hilo_reg.v |
| **MEM** | Memory Access | 访存、异常检测、异常提交 | DM.v, CP0_Reg.v |
| **WB** | Write Back | 选择写回数据、写寄存器 | regfile.v |

### 5.2 B/J类指令在各阶段的关键特性

#### IF阶段
- 正常取指，包括分支/跳转指令本身和延迟槽指令

#### ID阶段
- **分支判断提前**：Branch_Cmp在ID阶段完成，减少分支延迟
- **目标地址计算**：分支目标、跳转目标在ID阶段确定
- **返回地址计算**：PC+8为链接指令的返回地址
- **PC选择逻辑**：根据分支/跳转结果选择下一条指令地址
- **冒险检测**：检测分支/跳转寄存器冲突，必要时停顿

#### EX阶段
- **执行延迟槽指令**：分支/跳转的延迟槽指令在EX阶段执行
- **传递返回地址**：链接指令的PC+8通过流水线传递

#### MEM阶段
- **延迟槽指令的内存操作**（如果有）
- **传递返回地址**：继续传递PC+8

#### WB阶段
- **写回返回地址**：链接指令将PC+8写入寄存器（$31或rd）

### 5.3 设计亮点

1. **提前分支判断**：在ID阶段完成分支条件判断，有效分支延迟仅1个周期
2. **延迟槽机制**：支持MIPS标准延迟槽，提高指令吞吐率
3. **完善的冒险处理**：
   - 数据前推（EX、MEM、WB阶段）
   - 智能停顿（Load-Use、分支冒险）
   - 精确的冲刷控制
4. **统一的控制流**：所有B/J类指令在ID阶段完成控制转移决策
5. **精确异常支持**：异常在MEM阶段提交，保证指令语义的精确性

---

**适用场景**：本文档适用于本科生毕业设计答辩，全面展示MIPS五级流水线CPU的架构设计和实现细节。
