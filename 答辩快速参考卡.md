# 答辩快速参考卡

> 本文档为毕业设计答辩准备的快速参考，提供关键问题的简明答案。

---

## 一、B/J类指令快速参考

### 指令清单

| 类型 | 指令 | Opcode | 功能 | 写寄存器 |
|------|------|--------|------|---------|
| **B类** | BEQ | 000100 | rs == rt 则跳转 | 否 |
| | BNE | 000101 | rs != rt 则跳转 | 否 |
| | BGTZ | 000111 | rs > 0 则跳转 | 否 |
| | BLEZ | 000110 | rs <= 0 则跳转 | 否 |
| | BLTZ | 000001(00000) | rs < 0 则跳转 | 否 |
| | BGEZ | 000001(00001) | rs >= 0 则跳转 | 否 |
| | BLTZAL | 000001(10000) | rs < 0 则跳转 | $31 = PC+8 |
| | BGEZAL | 000001(10001) | rs >= 0 则跳转 | $31 = PC+8 |
| **J类** | J | 000010 | 无条件跳转 | 否 |
| | JAL | 000011 | 无条件跳转 | $31 = PC+8 |
| | JR | 000000(001000) | 跳转到rs | 否 |
| | JALR | 000000(001001) | 跳转到rs | rd = PC+8 |

### 指令定义位置

```
maindec2.v    → B类(BEQ/BNE/BGTZ/BLEZ/BLTZ/BGEZ/BLTZAL/BGEZAL), J/JAL
controller2.v → JR/JALR (特殊检测)
Branch_Cmp.v  → 分支条件比较逻辑
```

### 各模块作用（一句话概括）

| 模块 | 核心作用 |
|------|---------|
| **maindec2.v** | 根据opcode/rt译码，生成19位控制信号 |
| **controller2.v** | 管理控制信号流水线传递，处理JR/JALR |
| **Branch_Cmp.v** | ID阶段比较rs和rt，输出分支条件结果 |
| **datapath.v** | 实现五级流水线数据通路和PC控制逻辑 |
| **hazard.v** | 检测数据冒险，生成前推/停顿信号 |

---

## 二、五级流水线快速参考

### 五个阶段总览

| 阶段 | 名称 | 主要功能 | 对B/J类指令的特殊处理 |
|------|------|----------|---------------------|
| **IF** | 取指 | 根据PC取指令，计算PC+4 | 取分支/跳转指令和延迟槽指令 |
| **ID** | 译码 | 译码、读寄存器、**分支判断**、地址计算 | **在此完成分支判断和跳转决策** |
| **EX** | 执行 | ALU运算、前推、HI/LO操作 | 执行延迟槽指令 |
| **MEM** | 访存 | 内存读写、异常提交 | 传递PC+8（链接指令） |
| **WB** | 写回 | 选择数据写入寄存器 | 写PC+8到$31或rd（链接指令） |

### 各阶段实现位置

| 阶段 | datapath.v行号 | 关键模块 |
|------|---------------|---------|
| **IF** | 152-157 | pc.v, adder.v |
| **ID** | 159-353 | maindec2.v, Branch_Cmp.v, regfile.v |
| **EX** | 355-447 | alu.v, divider.v, hilo_reg.v |
| **MEM** | 449-539 | DM.v, CP0_Reg.v |
| **WB** | 541-584 | regfile.v (写操作) |

---

## 三、关键设计亮点（答辩必提）

### 1. 提前分支判断
```
传统设计: 分支判断在EX阶段 → 分支延迟2-3周期
本设计:   分支判断在ID阶段 → 分支延迟1周期（延迟槽）
```

**实现方法**：
- 在ID阶段集成Branch_Cmp模块
- 从MEM阶段前推数据到ID阶段
- 检测冲突时停顿1周期

### 2. 延迟槽机制
```
指令序列:  BEQ $2, $3, LABEL
          ADD $4, $5, $6    # 延迟槽，总是执行
          ...
LABEL:    SUB $7, $8, $9    # 目标指令
```

**关键特性**：
- 延迟槽指令总是执行，无论分支是否跳转
- 正常跳转时不冲刷流水线
- 减少控制冒险带来的性能损失

### 3. 完善的冒险处理

**前推机制**：
```
EX阶段前推: MEM/WB → EX (ALU操作数)
ID阶段前推: MEM → ID (分支/跳转操作数)
```

**停顿机制**：
```
Load-Use冒险:   停顿1周期
分支-ALU冒险:   停顿1周期
分支-Load冒险:  停顿1周期
```

### 4. 精确异常
```
异常检测: D阶段
异常传递: D → E → M (通过流水线)
异常提交: M阶段 (更新CP0, 跳转异常入口)
```

---

## 四、常见问题快速解答

### Q1: 为什么BEQ在ID阶段就能判断？
**A**: 在ID阶段已经：
1. 读取了rs和rt寄存器的值
2. 通过Branch_Cmp模块比较两个值
3. 如果前面有冒险，通过前推或停顿解决
4. 所以能立即得出分支条件结果

### Q2: 延迟槽指令会被取消吗？
**A**: 不会。延迟槽指令总是执行，这是MIPS架构的设计特性：
- 优点：减少控制冒险损失，简化流水线控制
- 缺点：需要编译器小心填充延迟槽

### Q3: JAL的返回地址为什么是PC+8？
**A**: 因为延迟槽机制：
- PC: JAL指令地址
- PC+4: 延迟槽指令地址（总是执行）
- PC+8: 返回时应该执行的指令地址
- 所以返回地址保存PC+8，跳过延迟槽

### Q4: 如何处理JR的数据冒险？
**A**: 
1. hazard.v检测：如果前面指令会修改JR的rs寄存器
2. 从MEM阶段前推rs的值（如果可用）
3. 如果不可用（EX阶段或Load），停顿1周期
4. 下周期前推后，JR正常执行

### Q5: 分支预测在哪里？
**A**: 本设计不需要分支预测：
- 采用延迟槽机制，延迟槽指令总是执行
- 分支判断在ID阶段完成，非常早
- 有效分支延迟仅1周期，无需预测

### Q6: 控制信号如何流过流水线？
**A**: 通过流水线寄存器（floprc）：
```
ID阶段: maindec2/controller2生成控制信号
  ↓ floprc (ID/EX寄存器)
EX阶段: 控制信号指导ALU等模块
  ↓ flopr (EX/MEM寄存器)
MEM阶段: 控制内存访问
  ↓ flopr (MEM/WB寄存器)
WB阶段: 控制寄存器写入
```

---

## 五、时序图速记

### BEQ跳转时序
```
T0: IF(BEQ)
T1: ID(BEQ判断) + IF(延迟槽)
T2: ID(延迟槽) + IF(目标指令)
```

### JAL跳转时序
```
T0: IF(JAL)
T1: ID(JAL计算PC+8) + IF(延迟槽)
T2: EX(延迟槽, PC+8传递)
...
T5: WB(PC+8 → $31)
```

### Load-Use冒险时序
```
T0: IF(LW)
T1: ID(LW) + IF(ADD)
T2: EX(LW) + ID(ADD) → 检测到冒险
T3: MEM(LW) + ID(ADD停顿) + EX(NOP气泡)
T4: WB(LW) + EX(ADD前推)
```

---

## 六、答辩PPT建议结构

### 第一部分：项目介绍 (2分钟)
- CPU架构：五级流水线 + 延迟槽
- 支持指令：55条（含12条B/J类）
- 关键特性：提前分支判断 + 完善冒险处理

### 第二部分：B/J类指令实现 (5分钟)
- 指令清单和编码
- 各模块作用（配架构图）
- 分支判断流程（配时序图）
- 冒险处理示例

### 第三部分：五级流水线详解 (5分钟)
- 各阶段功能总览（配流水线图）
- 针对B/J类指令的每阶段说明
- 流水线寄存器和控制信号传递

### 第四部分：设计亮点 (3分钟)
- 提前分支判断（对比传统设计）
- 延迟槽机制（性能优势）
- 完善的冒险处理（正确性保证）
- 精确异常支持

---

## 七、演示建议

### 推荐演示场景

**场景1：BEQ指令执行**
```assembly
     ADD  $2, $3, $4    # 计算rs值
     NOP                # 等待数据可用
     BEQ  $2, $5, L1    # 分支判断
     ADD  $6, $7, $8    # 延迟槽
     SUB  $9, $10, $11  # 不跳转时执行
L1:  AND  $12, $13, $14 # 跳转目标
```
**展示要点**：
- ID阶段分支判断
- 延迟槽指令总是执行
- 时序波形图

**场景2：JAL调用和返回**
```assembly
     JAL  FUNC          # 调用函数
     ADD  $2, $3, $4    # 延迟槽
     ...                # 返回后执行
FUNC:
     SUB  $5, $6, $7    # 函数代码
     JR   $31           # 返回
     NOP                # 延迟槽
```
**展示要点**：
- PC+8保存到$31
- JR读取$31跳转
- 函数调用完整流程

**场景3：Load-Use冒险**
```assembly
     LW   $2, 0($3)     # Load
     ADD  $4, $2, $5    # 使用$2，需停顿
```
**展示要点**：
- hazard.v检测冒险
- 停顿1周期
- MEM阶段前推数据

---

## 八、快速检查清单

答辩前确认以下内容准备完毕：

### 文档材料
- [ ] README.md（项目概览）
- [ ] 文档_分支跳转指令实现说明.md（B/J类指令详解）
- [ ] 文档_五级流水线详解.md（流水线详解）
- [ ] 答辩PPT（包含架构图、时序图、代码片段）

### 技术理解
- [ ] 能解释12条B/J类指令的编码和功能
- [ ] 能画出五级流水线架构图
- [ ] 能解释延迟槽机制和优势
- [ ] 能解释提前分支判断的实现
- [ ] 能解释冒险检测和处理机制
- [ ] 能画出关键指令的时序图

### 代码熟悉度
- [ ] 知道maindec2.v中指令译码的位置
- [ ] 知道Branch_Cmp.v的比较逻辑
- [ ] 知道hazard.v的冒险检测逻辑
- [ ] 知道datapath.v各阶段的实现位置
- [ ] 能找到流水线寄存器的定义

### 演示准备
- [ ] 仿真环境搭建完成
- [ ] 测试程序编写完成（包含B/J类指令）
- [ ] 波形显示配置完成（PC、指令、寄存器、控制信号）
- [ ] 截图/录屏准备完成

---

**祝答辩顺利！🎓**

记住：自信、清晰、逻辑严谨是成功答辩的关键。
